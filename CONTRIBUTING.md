# 參與這個專案

先講清楚期待：**這是為兩個人做的 app，不是通用產品。** 它每天被真的拿來記帳，
所以任何變更的第一考量是「會不會弄壞正在用的那本帳」。

## 很歡迎的

- **Bug 回報**，尤其是我手上兩台裝置驗不到的：
  - 掃發票在某支手機上讀不到（請附機型、瀏覽器版本、是不是安裝到主畫面）
  - iOS / Android 的行為差異（鍵盤遮擋、相機全螢幕、狀態列、安全區）
  - 同步連不上或連上後卡住
  - 無障礙問題——**低視力＋系統字級開到最大是本專案的主要情境**，
    任何「字放大之後就爛掉」的回報都是高優先
- 文件的錯字、講不通的地方、過期的說明
- 已回報 bug 的修正 PR

回報時請附上設定頁最下方的「複製診斷資訊」——那是純本機的錯誤環形日誌，
不含任何帳目內容。

## 原則上不收的功能請求

`docs/BACKLOG.md` 前半是這個專案的設計原則。下面這些**不是還沒排到，是已經被否決**，
而且理由寫得比這裡詳細，開 issue 之前請先讀：

| 不做 | 為什麼（詳見 BACKLOG） |
|---|---|
| 分帳、應收應付、「誰欠誰」 | 原則一：**債指向未來、會產生義務，義務產生槓桿**。加上應收應付的那一刻，帳就變成債本。這是憲法級的線 |
| 「這個圈已經 N 天沒更新」之類的提醒 | 原則四：材料是真的，但那會變成一台機器在提醒你失去了什麼 |
| 自動清理墓碑以節省空間 | 原則四：**永遠不要清**。而且算過了，10 年也只佔 1–3% |
| 雲端同步、帳號、跨網路同步 | 沒有伺服器是這個 app 的**本體**，不是還沒做的功能 |
| 把有發票背書的比例拿來評分或跨人比較 | 原則六：任何評分都會變成脅迫的槓桿 |

還有一份「量過或想清楚了、不要再提」的清單在 BACKLOG 末尾——包括匯出分段組字串
（做了、量了、慢 2.3 倍，退掉）與統計清單虛擬化。

如果你覺得某條原則是錯的，那是個值得聊的話題，但請當成**討論原則**來開，
不要當成功能請求來開。

## 送 PR 之前

```bash
pnpm lint && pnpm typecheck && pnpm test && pnpm build
```

四個都必須綠。CI 會跑同一組。

另外幾條會讓 PR 被退回的硬規則（完整版在 `CLAUDE.md`）：

- **`packages/core` 必須維持純粹**：零依賴、零 I/O、決定論。ESLint 機器強制禁用
  `window`／`fetch`／`Math.random`／`Date.now`／`new Date()`／`Intl`／`localeCompare`。
  時間一律以 `wallMs` 參數餵入
- **不改寫既有資料**。任何「順手補個欄位／正規化舊列」都會產生新的 HLC 信封，
  在對方手機上呈現為一批意義不明的編輯。要加欄位就設計成可缺席、讀取時 `?? 預設`
- **顏色一律住 `styles/base.css` 的 `:root`**，CSS 葉檔禁裸色值（有測試守著）
- **觸控目標寫成 `max(44px, …)`** 而不是裸 `44px`——系統字級放大時要跟著長
- 註解寫「為什麼」不是「做了什麼」，繁體中文

## 提醒

`main` 有 Cloudflare Workers Builds 綁著，合併就是部署到正在被使用的正式站。
所以 PR 不會被匆促合併，即使它是對的。
